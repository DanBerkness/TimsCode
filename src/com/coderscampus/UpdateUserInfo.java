package com.coderscampus;

public abstract class UpdateUserInfo {
	
	// Create methods to update UserInfo to be sorted as normal User or Super User when logged in
	
//	   promptUsernameToUpdate();
//	   promptUpdateName();
//	   promptUpdateUsername();
//	   promptUpdatePassword();
//	   promptUpdateUsername();
//	   promptOptions();
	   

	//  When logged in as a Normal User, you should be able to update any of the
	// fields that belong to this user instance
	// except the rol
	// should be prompted with option to update username, password, or name

	// User updates should output this information back to users.txt file and
	// make sure that the information is
	// properly sorted.

	//  When logged in as Super User you have same options as Normal User, have
	// ability to login as any other User
	// once switched to another User unable to switch back to SUPER user account

	// Sort super users before normal users

	// Sort usernames in alphabetical order(ascending order) smallest to the
	// largest
	
//	userService.getUserInfo(FileService.sortUsersFromFile("users.txt"));
}
